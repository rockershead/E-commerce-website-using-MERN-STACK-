const {User} = require('./user')
const {Product} =require('./product')
const {Session} =require('./session')
const {GoogleUser} =require('./googleUser')


module.exports={User,Product,Session,GoogleUser}